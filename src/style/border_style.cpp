#include "../../include/style/border_style.hpp"


re::Style::BorderStyle::BorderStyle(
    const sf::Color& color,
    const std::size_t& thickness
) : color(color),
    thickness(thickness) {

    }

